.table-toolbar {
    position: relative;
    background-color: #f8f8f8;
    border-bottom: 1px solid #ddd;
    padding: 8px 16px;

    clearfix();
}

.tfoot {
    .table-toolbar {
        border-bottom: none;
        border-top: 1px solid #ddd;
    }
}
